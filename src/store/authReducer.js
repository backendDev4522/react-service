
const initailState = {
    user: null,

}
export default function authReducer(state = initailState, action) {
    switch (action.type) {


        default:
            return state;
    }
}